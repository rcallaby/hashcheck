{
    "hashcat_path": "/usr/local/bin/hashcat",  // Path to hashcat binary
    "wordlist": "/path/to/custom/wordlist.txt",  // Custom wordlist path
    "options": "--force --potfile-disable"  // Additional hashcat options
}
